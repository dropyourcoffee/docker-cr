version: '2.0'
services:
  registry:
    image: registry:2
    restart: always
    volumes:
    - ./config.yml:/etc/docker/registry/config.yml
    - ./registry:/var/lib/registry
    ports:
    - 5000:5000
    network_mode: host

  web:
    build:
      context: ..
      dockerfile: Dockerfile
    depends_on:
      - apiserver
    ports:
    - 80:80

